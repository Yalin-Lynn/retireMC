% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_wealth.R
\name{simulate_wealth}
\alias{simulate_wealth}
\title{Simulate wealth trajectories given returns, contributions and withdrawals}
\usage{
simulate_wealth(
  initial,
  returns,
  contrib = 0,
  withdraw = 0,
  withdraw_timing = c("start", "end"),
  floor_zero = TRUE
)
}
\arguments{
\item{initial}{numeric initial wealth (W0)}

\item{returns}{matrix of gross returns (n_years x n_sims), as returned by simulate_returns}

\item{contrib}{numeric scalar or vector length n_years (positive = deposit)}

\item{withdraw}{numeric scalar or vector length n_years (positive = cash out)}

\item{withdraw_timing}{character "start" or "end" indicating when withdrawal occurs within year (default "start")}

\item{floor_zero}{logical; if TRUE, wealth floors at 0 (no negative balances)}
}
\value{
matrix of wealth values (n_years + 1 rows x n_sims columns). Row 1 is initial wealth.
}
\description{
Simulate wealth trajectories given returns, contributions and withdrawals
}
\examples{
R <- simulate_returns(30, 100, seed = 1)
simulate_wealth(1e6, R, contrib = 0, withdraw = 60000)
}
